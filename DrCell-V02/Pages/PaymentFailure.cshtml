@{
    ViewData["Title"] = "Pago Fallido";
}

<div class="container mx-auto px-4 py-8">
    <div class="max-w-md mx-auto bg-white rounded-lg shadow-lg p-6">
        <div class="text-center">
            <div class="mx-auto flex items-center justify-center h-12 w-12 rounded-full bg-red-100 mb-4">
                <svg class="h-6 w-6 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
            </div>
            
            <h1 class="text-2xl font-bold text-red-600 mb-2">Pago No Completado</h1>
            <p class="text-gray-600 mb-6">Hubo un problema al procesar tu pago. Por favor, intenta nuevamente.</p>
            
            @if(ViewBag.PaymentInfo != null)
            {
                <div class="bg-gray-50 p-4 rounded-lg mb-6 text-left">
                    <h3 class="font-semibold text-gray-700 mb-2">Información del Intento</h3>
                    <p><strong>Estado:</strong> @ViewBag.PaymentInfo.status</p>
                    @if(!string.IsNullOrEmpty(ViewBag.PaymentInfo.external_reference))
                    {
                        <p><strong>Referencia:</strong> @ViewBag.PaymentInfo.external_reference</p>
                    }
                </div>
            }
            
            <div class="space-y-2">
                <button onclick="history.back()" class="block w-full bg-blue-600 text-white py-2 px-4 rounded-lg hover:bg-blue-700 transition-colors">
                    Intentar Nuevamente
                </button>
                <a href="/" class="block w-full bg-gray-200 text-gray-700 py-2 px-4 rounded-lg hover:bg-gray-300 transition-colors">
                    Volver al Inicio
                </a>
            </div>
        </div>
    </div>
</div>
