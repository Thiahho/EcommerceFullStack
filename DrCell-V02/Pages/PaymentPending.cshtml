@{
    ViewData["Title"] = "Pago Pendiente";
}

<div class="container mx-auto px-4 py-8">
    <div class="max-w-md mx-auto bg-white rounded-lg shadow-lg p-6">
        <div class="text-center">
            <div class="mx-auto flex items-center justify-center h-12 w-12 rounded-full bg-yellow-100 mb-4">
                <svg class="h-6 w-6 text-yellow-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
            </div>
            
            <h1 class="text-2xl font-bold text-yellow-600 mb-2">Pago Pendiente</h1>
            <p class="text-gray-600 mb-6">Tu pago está siendo procesado. Te notificaremos cuando se complete.</p>
            
            @if(ViewBag.PaymentInfo != null)
            {
                <div class="bg-gray-50 p-4 rounded-lg mb-6 text-left">
                    <h3 class="font-semibold text-gray-700 mb-2">Información del Pago</h3>
                    <p><strong>Estado:</strong> @ViewBag.PaymentInfo.status</p>
                    @if(!string.IsNullOrEmpty(ViewBag.PaymentInfo.payment_id))
                    {
                        <p><strong>ID de Pago:</strong> @ViewBag.PaymentInfo.payment_id</p>
                    }
                    @if(!string.IsNullOrEmpty(ViewBag.PaymentInfo.external_reference))
                    {
                        <p><strong>Referencia:</strong> @ViewBag.PaymentInfo.external_reference</p>
                    }
                </div>
            }
            
            <div class="bg-blue-50 p-4 rounded-lg mb-6">
                <p class="text-sm text-blue-700">
                    <strong>¿Qué sigue?</strong><br>
                    - Recibirás un email de confirmación cuando el pago se complete<br>
                    - Puedes consultar el estado en tu área de cliente<br>
                    - Si tienes dudas, contacta nuestro soporte
                </p>
            </div>
            
            <div class="space-y-2">
                <a href="/" class="block w-full bg-blue-600 text-white py-2 px-4 rounded-lg hover:bg-blue-700 transition-colors">
                    Volver al Inicio
                </a>
                <a href="/contacto" class="block w-full bg-gray-200 text-gray-700 py-2 px-4 rounded-lg hover:bg-gray-300 transition-colors">
                    Contactar Soporte
                </a>
            </div>
        </div>
    </div>
</div>
